# Installation

Install the package with your tool of choice:

```bash
pip install acme-widget
# or
uv add acme-widget
```

Verify the install:

```python
import acme_widget

print(acme_widget.__version__)
```

> This document’s heading styles, code blocks, and blockquote are all KPress styling.
> The page frame around it belongs to the outer site.
